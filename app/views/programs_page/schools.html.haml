= content_for :title do
  programs

= content_for :sidemenu do
  = render 'programs_page/sidebar'

= content_for :main do
  .tabbable
    %ul#school-tabs.nav.nav-tabs
      - if current_user
        = school_tabs_for(@schools)
    .tab-content
      = school_tab_panes_for(@schools, @programs)
  %a.btn.btn-primary.new-program{:href => "/programs_page/new"}

    New Program
  = modal_for "new-program", :modal_title => "new program"
