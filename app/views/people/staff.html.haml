= content_for :title do
	people

= content_for :sidemenu do
	= render 'people/sidebar'

= content_for :main do
	%table.table.table-bordered.table-striped{:style => 'clear: both'}
		%tr
			%th{:width => '25%'}
				First Name
			%th{:width => '25%'}
				Last Name
			%th{:width => '25%'}
				Email
			%th{:width => '25%'}
				Region
		-@staffs.each do |staff|
			%tr
				%td
					%a.first-name-input{:href => '#'}
						= staff.first_name
				%td
					%a.last-name-input{:href => '#'}
						= staff.last_name
				%td
					%a.email-input{:href => '#'}
						= staff.email
				%td
					%a.region-input{:href => '#'}
	%div.container	
		%div{:class => 'row' , :style => 'padding-top: 240px;'}
			%a{:href =>"#",:class =>"btn btn-large btn-primary",:rel =>"popover", :data => {'content' => "#{render 'people/popform'}",'placement' =>"top", 'original-title' =>"Fill in form"}} CREATE

/ http://jsfiddle.net/7e2XU/21/
/ "#{render 'people/popform'}"