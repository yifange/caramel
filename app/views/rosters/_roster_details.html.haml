%tr
  %td
    - date = course.date
    %a{:href => "/courses?program_id=" + @program_id.to_s + "&year=" + date.year.to_s + "&month=" + date.month.to_s + "&day=" + date.day.to_s}
      = content_tag :p, course.summary[1]
      = content_tag :p, course.summary[2]
  %td
    %a.x-editable.roster-date{:href => "#", "data-pk" => "#{roster.id}", "data-value" => roster.start_date}
  %td
    %a.x-editable.roster-date{:href => "#", "data-pk" => "#{roster.id}", "data-value" => roster.end_date}
  %td
    %a.x-editable.roster-notes{:href => "#", "data-type" => "textarea", "data-pk" => "#{roster.id}", "data-name" => "notes"}
